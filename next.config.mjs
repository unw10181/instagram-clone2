/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "www.doi.gov",
      "storage.googleapis.com",
      "images.unsplash.com",
      "res.cloudinary.com",
    ],
  },
};

export default nextConfig;
